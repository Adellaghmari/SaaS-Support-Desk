# Server
PORT=3001
NODE_ENV=development

# PostgreSQL (Neon, Docker, or local)
# Neon example:
# DATABASE_URL=postgresql://user:password@host/database?sslmode=require
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/saas_support_desk

# Force in memory database for local demo (no PostgreSQL required)
# USE_MEMORY_DB=true

# CORS: frontend URL (Vercel URL in production)
FRONTEND_URL=http://localhost:5173
